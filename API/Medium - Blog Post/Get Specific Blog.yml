info:
  name: Get Specific Blog
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/blog"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
