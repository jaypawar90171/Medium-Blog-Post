info:
  name: Create Blog
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/signin"
  body:
    type: json
    data: |-
      {
        "email": "jayp90171@gmail.com",
        "password": "Jay@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
